/* Use the following commands to generate ppTest.sbv:

     :set sbv
     :fm ppTest "./ppTest.sbv"
*/

ppTest : ([8], [8]) -> [8];
ppTest (a, b) = g+g+1
  where { c = a+b;
          d = c+c;
          e = d+d;
          f = e+e;
          g = f+f;
        };
