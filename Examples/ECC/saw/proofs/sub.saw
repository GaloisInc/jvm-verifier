method com.galois.ecc.NIST64.sub
{
  var z, x, y :: int[12];
  mayAlias { z, x, y };
  let jx = join(valueOf(x));
  let jy = join(valueOf(y));
  ensure valueOf(z) := split(jx - jy) : [12][32]; 
  return if jx >=u jy then 0:[32] else -(1:[32]);
  verify abc;
};
