/* We can't even fully simulate this method. Bleh. Even on concrete
 * values! */
method com.galois.ecc.NIST64.sq
{
  var a :: int[24];
  var x :: int[12];
  mayAlias { a, x };
  let x' = join(valueOf(x)) : [384];
  ensure valueOf(a) := split(ref_safe_product(x', x')) : [24][32];
  quickcheck 10;
};
